import { loader as indexLoader, action as indexAction, default as IndexComponent } from "./app._index";

export const loader = indexLoader;
export const action = indexAction;
export default IndexComponent;
