// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

// Note that some adapters may set a maximum length for the String type by default, please ensure your strings are long
// enough when changing adapters.
// See https://www.prisma.io/docs/orm/reference/prisma-schema-reference#string for more information
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Session {
  id                  String    @id
  shop                String
  state               String
  isOnline            Boolean   @default(false)
  scope               String?
  expires             DateTime?
  accessToken         String
  userId              BigInt?
  firstName           String?
  lastName            String?
  email               String?
  accountOwner        Boolean   @default(false)
  locale              String?
  collaborator        Boolean?  @default(false)
  emailVerified       Boolean?  @default(false)
  refreshToken        String?
  refreshTokenExpires DateTime?
}

model ActivityLog {
  id           Int      @id @default(autoincrement())
  shop         String
  productId    String
  productTitle String
  productSku      String?
  productImageUrl String?
  method          String?  // "AUTO", "MANUAL", "WEBHOOK"
  action       String
  createdAt    DateTime @default(now())

  @@index([shop, createdAt])
}

model Settings {
  shop            String   @id
  isActive        Boolean  @default(false)
  frequency       Int      @default(1)
  frequencyUnit   String   @default("days") // "days" or "minutes"
  minDaysInactive Int      @default(90)
  autoReactivate  Boolean  @default(false)
  lastRunAt       DateTime?
  nextRunAt       DateTime?
  lastScanType    String?  // "AUTO" or "MANUAL"
  lastScanResults String?  // JSON string of products found/actioned (auto)
  lastManualRunAt DateTime?
  lastManualScanResults String? // JSON string of products found/actioned (manual)
  lastManualScanDays Int?  // Remember last manual scan days threshold
  currentStatus   String   @default("IDLE") // "IDLE", "SCANNING", "DEACTIVATING"
  gracePeriodEndsAt DateTime? // Set when billing lapses; cleared on resubscribe. Past this = hard gate.
  subscriptionStatus String @default("NONE") // "ACTIVE" | "GRACE" | "NONE". Authoritative for scheduler gating; webhook + loader keep it current.
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}
