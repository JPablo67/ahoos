name: inventory-deactivator-2

services:
  db:
    image: postgres:16-alpine
    container_name: inventory-db-2
    restart: always
    environment:
      POSTGRES_USER: inventory_app
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: inventory_deactivator
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5434:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U inventory_app -d inventory_deactivator"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  app:
    container_name: inventory-deactivator-2
    build: .
    restart: always
    ports:
      - "4502:3000"
    env_file: .env
    environment:
      SENTRY_RELEASE: ${SENTRY_RELEASE:-unknown}
    depends_on:
      db:
        condition: service_healthy

  pgadmin:
    image: dpage/pgadmin4
    container_name: inventory-pgadmin-2
    restart: always
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD}
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    ports:
      - "5052:80"

volumes:
  pgdata:
  pgadmin_data:
